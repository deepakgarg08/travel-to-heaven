/**
 * sample validation file - in case of post/put/patch etc request
 */
// import Joi from 'joi';

// const create = Joi.object({
//     name: Joi.string().required(),
//     address: Joi.string().required()
// });

// export default { create };
